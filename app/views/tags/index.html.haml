%h3="タグ検索結果#{@tags_count}件ヒット"

#aa-list
  -if @aas
    = render(partial: "aas/asciiArt", object: @aas)
    = will_paginate @asciiArts, previous_label: "<", next_label: ">"
  -else
    %p ヒットしませんでした。
