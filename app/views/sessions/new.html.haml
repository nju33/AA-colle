-if notice
  %p#notice= notice

%p{style: "text-decoration: line-through;"}= link_to "Twitterでログイン", "/auth/twitter"

=form_tag do

  %fieldset
    %legend ログイン

    .form
      =label_tag :email, 'メールアドレス'
      =email_field_tag :email, params[:email]
    .form
      =label_tag :password, 'パスワード'
      =password_field_tag :password, params[:password]
    -#=hidden_field_tag, name: "authenticity_token", value: "#{form_authenticity_token}"
    .action
      =submit_tag 'ログイン'

